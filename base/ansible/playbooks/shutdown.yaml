---
- hosts: tree
  strategy: free
  gather_facts: no
  tasks:
    - name: Kill pats process
      shell: "killall pats || true"
    - name: Shutdown
      become: true
      ignore_errors: yes
      ignore_unreachable: yes
      async: yes
      poll: no
      shell: "halt -p"
