version: '3.5'

services:
  pats_c:
    restart: unless-stopped
    build: ./pats_c
    ports:
      - "8001:8001"
    volumes:
      - /home/flask/pats_c
      - ~/patsc/static/:/home/flask/pats_c/static/
      - ~/patsc/db/:/home/flask/patsc/db/
      - ~/.ssh/:/home/flask/patsc/ssh_tmp
      - ../base/install/:/home/flask/patsc/base_install
      - ~/code/pats/config/:/home/flask/code/pats/config/
    command: gunicorn --workers=4 "pats_c:create_app()" -b :8001 --worker-tmp-dir /dev/shm

  nginx_beta_site:
    image: nginx
    restart: unless-stopped
    expose:
      - 80
      - 443
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ~/patsc/static/:/static
      - ./nginx_beta_site:/etc/nginx/conf.d
      - ./certbot_beta_site/conf:/etc/letsencrypt
      - ./certbot_beta_site/www:/var/www/certbot
    links:
      - pats_c
    depends_on:
      - pats_c
    command: "/bin/sh -c 'while :; do sleep 6h & wait $${!}; nginx -s reload; done & nginx -g \"daemon off;\"'"

  certbot_beta_site:
    image: certbot/certbot
    restart: unless-stopped
    volumes:
      - ./certbot_beta_site/conf:/etc/letsencrypt
      - ./certbot_beta_site/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
